<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.androidunleashed.foss">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FOSS"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.SplashScreen"
            android:theme="@style/AppTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserFiles.UserUpdate" />
        <activity android:name=".UserFiles.UserPofile" />
        <activity android:name=".PackUpdate.BusinessUpdate" />
        <activity android:name=".UI.BusinessProfile" />
        <activity android:name=".UI.OrderB" />
        <activity android:name=".UserFiles.MyCart" />
        <activity android:name=".UserFiles.confirmOrder" />
        <activity android:name=".UI.AddItem" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UI.BNewsFeed" />
        <activity android:name=".UI.verify_mail" />
        <activity android:name=".UI.BusinessSignup" />
        <activity android:name=".UI.DetailPage" />
        <activity android:name=".UI.UserSignup" />
        <activity android:name=".UI.BusinessLogin" />
        <activity android:name=".UI.UserLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>